---
import JobItem from "./JobItem.astro";
import type {IJob} from "@util/job.interface";

export interface Props {
    Jobs: IJob;
}
const jobs:any=Astro.props
---
jobs.map((job:IJob) => (
    <JobItem Job={job} />
))
